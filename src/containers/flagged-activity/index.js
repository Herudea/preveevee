import React from 'react';
import DataNav from '../../components/data-nav';

const FlaggedActivity = () => (
  <div>
    <DataNav />
    <h1>Flagged Activity Page</h1>
    <p>Did you get here via Redux?</p>
  </div>
);

export default FlaggedActivity;
